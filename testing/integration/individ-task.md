---

# 🧾 Техникалық тапсырма

## Тақырыбы: **Spring Boot қолдану арқылы интеграциялық тест жасау**

**Пән:** Бағдарламалық жасақтаманы тестілеу
**Тапсырма түрі:** Жеке жұмыс

---

## 🎯 **Мақсаты**

Студенттің Spring Boot фреймворкін пайдалана отырып, екі сервис (Order және Payment) арасындағы интеграциялық байланысты тестілеу дағдысын қалыптастыру.

---

## 📚 **Теориялық негіз**

Интеграциялық тестілеу — жүйенің екі немесе одан да көп модульдерінің өзара әрекеттесуін тексеру процесі.
Бұл тапсырмада студент **Order Service** пен **Payment Service** арасындағы интеграциялық байланысты Spring Boot Test арқылы тексереді.

---

## ⚙️ **Қолданылатын технологиялар мен құралдар**

| № | Құрал                | Міндеті                    |
| - | -------------------- | -------------------------- |
| 1 | **Java 21**          | Бағдарламалау тілі         |
| 2 | **Spring Boot 3.x**  | Қолданба фреймворкі        |
| 3 | **Maven**            | Тәуелділіктерді басқару    |
| 4 | **JUnit 5**          | Тестілеу кітапханасы       |
| 5 | **Spring Boot Test** | Интеграциялық тест жүргізу |

---

## 🧩 **Жүйе сипаттамасы**

Жоба екі негізгі қызметтен тұрады:

| Қызмет атауы        | Міндеті                                                                             |
| ------------------- | ----------------------------------------------------------------------------------- |
| **Order Service**   | Пайдаланушыдан тапсырыс қабылдайды және төлем нәтижесіне қарай мәртебені анықтайды. |
| **Payment Service** | Төлем сомасын тексереді және төлем сәтті/сәтсіз екенін қайтарады.                   |

---

## 🔁 **Логикалық байланыс**

1. `OrderController` тапсырысты қабылдайды (`amount` мәнімен).
2. Ол `PaymentService`-ке төлемді тексеру үшін сұраныс жібереді.
3. Егер `amount > 0`, онда тапсырыс **SUCCESS** күйінде,
   әйтпесе **FAILED** күйінде қайтарылады.

---

## 🧠 **Міндетті бизнес-логика**

| Жағдай       | Төлем сомасы (amount) | Күтілетін нәтиже               |
| ------------ | --------------------- | ------------------------------ |
| Сәтті төлем  | > 0                   | Тапсырыс мәртебесі = “SUCCESS” |
| Сәтсіз төлем | ≤ 0                   | Тапсырыс мәртебесі = “FAILED”  |

---

## 🔨 **Тапсырманы орындау қадамдары**

### 1. **Құрылым дайындау**

Жоба құрылымы төмендегідей болуы тиіс:

```
src/
 └── main/java/com/example/demo/
     ├── controller/
     │    └── OrderController.java
     ├── service/
     │    ├── OrderService.java
     │    └── PaymentService.java
 └── test/java/com/example/demo/
     └── IntegrationTest.java
```

---

### 2. **OrderController құру**

```java
@RestController
@RequestMapping("/orders")
public class OrderController {

    private final PaymentService paymentService;

    public OrderController(PaymentService paymentService) {
        this.paymentService = paymentService;
    }

    @PostMapping
    public ResponseEntity<String> createOrder(@RequestParam double amount) {
        boolean paymentSuccess = paymentService.processPayment(amount);
        if (paymentSuccess) {
            return ResponseEntity.ok("SUCCESS");
        } else {
            return ResponseEntity.ok("FAILED");
        }
    }
}
```

---

### 3. **PaymentService құру**

```java
@Service
public class PaymentService {
    public boolean processPayment(double amount) {
        return amount > 0;
    }
}
```

---

### 4. **Интеграциялық тест жазу**

Тест JUnit 5 және Spring Boot Test негізінде жазылады.
Тестте екі сценарий тексеріледі:

* `amount > 0` → күтілетін жауап `"SUCCESS"`
* `amount = 0` → күтілетін жауап `"FAILED"`

```java
@SpringBootTest
@AutoConfigureMockMvc
class IntegrationTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    void testSuccessfulOrder() throws Exception {
        mockMvc.perform(post("/orders")
                .param("amount", "100"))
                .andExpect(status().isOk())
                .andExpect(content().string("SUCCESS"));
    }

    @Test
    void testFailedOrder() throws Exception {
        mockMvc.perform(post("/orders")
                .param("amount", "0"))
                .andExpect(status().isOk())
                .andExpect(content().string("FAILED"));
    }
}
```

---

### 5. **Тест нәтижесін талдау**

| Тест атауы              | Сценарий   | Күтілетін нәтиже | Нақты нәтиже |
| ----------------------- | ---------- | ---------------- | ------------ |
| `testSuccessfulOrder()` | amount > 0 | SUCCESS          | SUCCESS ✅    |
| `testFailedOrder()`     | amount = 0 | FAILED           | FAILED ✅     |

---

## 💬 **Тапсыру форматы**

* Жоба `.zip` немесе GitHub репозиторий түрінде.
* Құрамында келесі файлдар болуы қажет:

  * `OrderController.java`
  * `PaymentService.java`
  * `IntegrationTest.java`
  * `pom.xml`

---

## 🧾 **Бағалау критерийлері**

| Критерий                                         | Балл        | Түсініктеме                                         |
| ------------------------------------------------ | ----------- | --------------------------------------------------- |
| Жоба құрылымы мен тәуелділіктердің дұрыстығы     | 2           | Maven конфигурациясы мен Spring Boot құрылымы дұрыс |
| OrderController және PaymentService логикасы     | 3           | Дұрыс бизнес логика іске асқан                      |
| Интеграциялық тест сценарийлері толық орындалған | 3           | Екі сценарий тексерілген (SUCCESS және FAILED)      |
| Тест нәтижелерін түсіндіру                       | 2           | Нәтижелер мен қорытынды нақты жазылған              |
| **Барлығы:**                                     | **10 балл** |                                                     |

---

## 📚 **Қосымша ресурстар**

* [Spring Boot Testing — Official Docs](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.testing)
* [JUnit 5 User Guide](https://junit.org/junit5/docs/current/user-guide/)
* [MockMvc Documentation](https://docs.spring.io/spring-framework/reference/testing/spring-mvc-test-framework.html)

---

## 🧠 **Бақылау сұрақтары**

1. **Интеграциялық тестілеу дегеніміз не және ол модульдік тесттен несімен ерекшеленеді?**
2. **Spring Boot Test аннотациясы қандай мақсатта қолданылады?**
3. **MockMvc класының рөлі қандай және ол не үшін қолданылады?**
4. **Order Service пен Payment Service арасындағы интеграциялық байланысты қалай ұйымдастыруға болады?**
5. **Интеграциялық тест кезінде қолданылатын ең жиі кездесетін аннотацияларды атаңыз және олардың қызметін түсіндіріңіз.**
6. **Тест сценарийінде “SUCCESS” және “FAILED” мәртебелері қандай логикалық шарттарға байланысты анықталады?**
7. **Spring Boot-та @SpringBootTest және @WebMvcTest аннотацияларының айырмашылығы неде?**
8. **IntegrationTest класында неге MockMvc қолданылады және оны қалай баптау керек?**
9. **Төлем сәтсіз болған жағдайда (мысалы, amount = 0) жүйе қандай әрекет жасауы тиіс және оны тестте қалай тексеруге болады?**
10. **Интеграциялық тестілеуді автоматтандыру қандай артықшылықтар береді және оны өндірістік ортада қалай қолдануға болады?**

