# Реализация API шлюза с помощью GraphQL

## Интерактивная практика

Для выполнения заданий можно использовать онлайн-песочницу GraphQL:  
👉 https://lucasconstantino.github.io/graphiql-online/

После загрузки страницы нажмите **“Change Endpoint”** и вставьте следующий URL:  
➡️ `https://graphqlzero.almansi.me/api`

---

## 📘 Примеры с решениями и обсуждением

### 1. 🔍 Базовая выборка полей и связанных данных
**Задача:** Получить идентификатор, имя, телефон и город проживания для пользователя с ID 2.
```graphql
query GetUserContactAndCity {
  user(id: "2") {
    id
    name
    phone
    address {
      city
    }
  }
}
```
**Обсуждение:** Используем вложенные поля. Поле `address` требуется, чтобы получить `city`.

---

### 2. 📝 Пагинация и общее количество элементов
**Задача:** Получить заголовки задач (todos) на второй странице (page: 2), по 5 элементов на странице, а также общее количество всех задач.
```graphql
query GetSecondPageOfTodos {
  todos(options: { paginate: { page: 2, limit: 5 } }) {
    data {
      title
    }
    meta {
      totalCount
    }
  }
}
```
**Обсуждение:** Параметр `paginate` в `options` управляет пагинацией. `meta.totalCount` возвращает общее количество задач.

---

### 3. 📉 Фильтрация с оператором “Меньше или Равно” (LTE)
**Задача:** Найти все записи (posts), ID которых меньше или равен 3.  
```graphql
query FilterPostsByMaxId {
  posts(options: { operators: [{ kind: LTE, field: "id", value: "3" }] }) {
    data {
      id
      title
      body
    }
  }
}
```
**Обсуждение:** Оператор `LTE` означает “Less Than or Equal” — меньше или равно.

---

### 4. ➕ Создание нового ресурса (альбома)
**Задача:** Создать новый альбом “Отпуск 2024” для пользователя с ID 1.  
```graphql
mutation CreateHolidayAlbum {
  createAlbum(input: { title: "Отпуск 2024", userId: "1" }) {
    id
    title
  }
}
```
**Обсуждение:** Используем мутацию `createAlbum`, передавая объект `input` с полями `title` и `userId`.

---

### 5. 🔁 Обновление существующего ресурса (поста)
**Задача:** Обновить пост с ID 5, изменив только его тело.  
```graphql
mutation UpdatePostContent {
  updatePost(id: "5", input: { body: "Обновлено: Срочная новость!" }) {
    title
    body
  }
}
```
**Обсуждение:** Параметр `input` позволяет передавать только изменяемые поля.

---

### 6. 🖼️ Вложенная выборка с аргументами
**Задача:** Для пользователя с ID 10 получить его имя и первые три альбома.  
```graphql
query GetUserAndFirstThreeAlbums {
  user(id: "10") {
    name
    albums(options: { slice: { limit: 3 } }) {
      data {
        title
      }
    }
  }
}
```
**Обсуждение:** Аргумент `slice` применяется к вложенному полю `albums` и ограничивает количество результатов.

---

### 7. 🧐 Множественная фильтрация и сортировка
**Задача:** Найти все комментарии, где ID поста не равен 50 и меньше или равен 60. Результаты отсортировать по email по возрастанию.
```graphql
query ComplexCommentFilter {
  comments(
    options: {
      operators: [
        { kind: NE, field: "postId", value: "50" }
        { kind: LTE, field: "postId", value: "60" }
      ]
      sort: [{ field: "email", order: ASC }]
    }
  ) {
    data {
      id
      email
    }
  }
}
```
**Обсуждение:** Массив `operators` объединяет условия через логическое “И”. `sort` определяет порядок сортировки.

---

### 8. 👤 Комплексное создание пользователя
**Задача:** Создать пользователя с вложенными данными о компании и адресе.  
```graphql
mutation CreateFullUser {
  createUser(
    input: {
      name: "Тестовый Разраб"
      username: "dev_test"
      email: "dev@test.com"
      company: { name: "GraphQL Solutions", catchPhrase: "Fast API" }
      address: { street: "Kek St." }
    }
  ) {
    id
    name
    company {
      name
    }
    address {
      street
    }
  }
}
```
**Обсуждение:** Используются вложенные объекты `company` и `address` внутри `CreateUserInput`.

---

### 9. ❌ Удаление ресурса (задачи)
**Задача:** Удалить задачу с ID 150.  
```graphql
mutation RemoveTodo {
  deleteTodo(id: "150")
}
```
**Обсуждение:** Мутация удаления возвращает булево значение, подтверждающее успех операции.

---

### 10. 🖼️ Обновление фотографии
**Задача:** Обновить URL фотографии с ID 55.  
```graphql
mutation UpdatePhotoUrl {
  updatePhoto(
    id: "55"
    input: { url: "https://new.photo.com/image55.jpg" }
  ) {
    id
    url
    title
  }
}
```
**Обсуждение:** Мы обновляем только поле `url`, оставляя остальные без изменений.

---

## 🎓 Практические задания для студентов

Выполните следующие задачи **самостоятельно**, опираясь на примеры выше.  
Используйте структуру GraphQL-запросов и мутаций, рассмотренных в разделе “Примеры с решениями и обсуждением”.  
Под каждой задачей приведена небольшая подсказка (**Hint**) — ссылка на схожий пример.

---

### 🚀 Queries — Получение данных

1. **Получить пользователя по ID**: запросить имя, email и название компании пользователя с id: `"5"`.  
   _Hint: см. задачу №1 — базовая выборка поля и связанных данных._

2. **Получить список записей с пагинацией**: вернуть первые 10 постов (posts) с их заголовками и идентификаторами пользователей.  
   _Hint: см. задачу №2 — пагинация и общее количество._

3. **Получить фотографии альбома**: запросить заголовок и URL превью всех фотографий в альбоме с id: `"42"`.  
   _Hint: см. задачу №10 — обновление фотографии с частичным input._

4. **Поиск и сортировка задач**: получить задачи (todos), отфильтрованные по title с оператором LIKE и значением `"qui"`, отсортированные по title в порядке убывания (DESC).  
   _Hint: см. задачу №7 — множественная фильтрация и сортировка._

5. **Получить комментарии к записи**: для поста с id: `"15"` получить имена и текст всех комментариев.  
   _Hint: см. задачу №3 — фильтрация с оператором LTE._

6. **Получить данные о компании пользователя**: запросить имя пользователя с id: `"8"` и название его компании.  
   _Hint: см. задачу №8 — создание пользователя с вложенными объектами._

7. **Запрос с оператором сравнения**: получить альбомы, где userId больше или равен `"7"`, вернуть id и title альбомов.  
   _Hint: см. задачу №3 — фильтрация по ID (LTE/GTE)._

8. **Детали адреса пользователя**: получить имя пользователя с id: `"1"` и его полный адрес (улица, город, индекс).  
   _Hint: см. задачу №1 — вложенные поля в user.address._

9. **Получить общее количество элементов**: запросить totalCount всех пользователей и записей.  
   _Hint: см. задачу №2 — использование meta.totalCount._

10. **Проверка статуса задачи**: получить заголовок и статус завершённости (completed) задачи с id: `"200"`.  
    _Hint: см. задачу №4 — создание ресурса и возврат конкретных полей._

---

### 🛠️ Mutations — Изменение данных

1. **Создать новую запись (Post)**: создать пост с title: `"Мой первый пост"` и body: `"Это тело нового поста"`. Вернуть id и title.  
   _Hint: см. задачу №4 — создание альбома._

2. **Обновить альбом**: изменить title альбома с id: `"10"` на `"Новый заголовок альбома"`, вернуть обновлённый объект.  
   _Hint: см. задачу №5 — обновление существующего ресурса._

3. **Удалить комментарий**: удалить комментарий с id: `"500"`, вернуть булево значение результата.  
   _Hint: см. задачу №9 — удаление ресурса._

4. **Создать пользователя**: создать пользователя с name: `"Иван Иванов"`, username: `"ivan_i"`, email: `"ivan@example.com"`. Вернуть id и email.  
   _Hint: см. задачу №8 — создание комплексного пользователя._

5. **Обновить задачу (Todo)**: обновить задачу с id: `"100"`, установив completed: true.  
   _Hint: см. задачу №5 — частичное обновление полей._

6. **Удалить фотографию**: удалить фото с id: `"600"`.  
   _Hint: см. задачу №9 — мутация deleteTodo._

7. **Создать комментарий**: создать комментарий с name, email и body. Вернуть id и name созданного комментария.  
   _Hint: см. задачу №4 — структура createX(input: {...})._

8. **Обновить почту пользователя**: изменить email пользователя с id: `"3"` на `"new.email@updated.com"`.  
   _Hint: см. задачу №5 — обновление одного поля через updateX(id, input)._
   
9. **Создать фотографию**: добавить новую фотографию с title, url и thumbnailUrl. Вернуть id и title.  
   _Hint: см. задачу №10 — обновление фотографии._

10. **Удалить запись (Post)**: удалить запись с id: `"90"`.  
    _Hint: см. задачу №9 — удаление ресурса._

---
